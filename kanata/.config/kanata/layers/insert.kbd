(deflayer insert
  grv  1    2    3    4    5    6    7    8    9    0    -    =    bspc
  tab  q    w    e    r    t    y    u    i    o    p    [    ]    \
  caps a    s    d    f    g    h    j    k    l    ;    '    ret
  lsft z    x    c    v    b    n    m    ,    .    /    rsft
  lctl spc  lalt           @spc_meta            ralt rmet @speed
)
(defalias spc_meta (tap-hold $tt $ht spc lmet ))
;; Unused keys: lmet, rmet, caps-hold, tab-hold, lsft-tap, rsft-tap, ralt-hold, rctl, lalt-tap, lctl-tap
